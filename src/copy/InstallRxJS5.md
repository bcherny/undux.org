ts
```ts
// Using Yarn:
yarn add undux rxjs@^5

// Or, using NPM:
npm install undux rxjs@^5 --save
```

flow
```ts
// Using Yarn:
yarn add undux rxjs@^5

// Or, using NPM:
npm install undux rxjs@^5 --save
```

es6
```ts
// Using Yarn:
yarn add undux rxjs@^5

// Or, using NPM:
npm install undux rxjs@^5 --save
```

es5
```ts
// Using Yarn:
yarn add undux rxjs@^5

// Or, using NPM:
npm install undux rxjs@^5 --save
```
