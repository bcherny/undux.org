ts
```ts
// Using Yarn:
yarn add undux@^3

// Or, using NPM:
npm install undux@^3 --save
```

flow
```ts
// Using Yarn:
yarn add undux@^3

// Or, using NPM:
npm install undux@^3 --save
```

es6
```ts
// Using Yarn:
yarn add undux@^3

// Or, using NPM:
npm install undux@^3 --save
```

es5
```ts
// Using Yarn:
yarn add undux@^3

// Or, using NPM:
npm install undux@^3 --save
```
