ts
```ts
let setKittens = store.set('kittens')
setKittens(Infinity)
```

flow
```js
let setKittens = store.set('kittens')
setKittens(Infinity)
```

es6
```js
let setKittens = store.set('kittens')
setKittens(Infinity)
```

es5
```js
var setKittens = store.set('kittens')
setKittens(Infinity)
```
